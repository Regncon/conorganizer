'use client';
import { touchRippleClasses } from '@mui/material';
import TouchRipple from '@mui/material/ButtonBase/TouchRipple';

type Props = {};

const Test = ({}: Props) => {
    return <TouchRipple></TouchRipple>;
};

export default Test;
