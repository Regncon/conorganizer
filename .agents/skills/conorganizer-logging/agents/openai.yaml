interface:
  display_name: "Conorganizer Logging"
  short_description: "Logging and error handling with slog in Go and templ code"
  default_prompt: "Update or add logging in conorganizer using the existing slog JSON setup, canonical fields, and privacy guardrails."
