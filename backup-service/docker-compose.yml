services:
    backup-service:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: backup-dev
        volumes:
            - .:/data
        env_file:
            - .env
        restart: unless-stopped
