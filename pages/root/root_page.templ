package root

import (
	"database/sql"
	"time"

	"github.com/Regncon/conorganizer/components"
	"github.com/Regncon/conorganizer/service/program"
)

templ rootPage(db *sql.DB, eventImageDir *string, requestedDate string, interests billettholderInterestsByEvent) {
	<div id="root-page-wrapper">
		@rootPageContentForDate(db, eventImageDir, requestedDate, interests)
	</div>
}

templ rootPageContent(db *sql.DB, eventImageDir *string) {
	@rootPageContentForDate(db, eventImageDir, "", nil)
}

templ rootPageContentForDate(db *sql.DB, eventImageDir *string, requestedDate string, interests billettholderInterestsByEvent) {
	{{ programPublished, err := program.IsPublished(db) }}
	@components.Breadcrumbs([]components.BreadcrumbPath{
		{Name: "Hjem", Url: ""},
	})
	if err != nil {
		<div class="page-content-container">
			<p class="root-page-error">{ rootPageLoadErrorMessage }</p>
		</div>
	} else {
		if programPublished {
			{{ programDays, programDaysErr := program.GetDays(db) }}
			if programDaysErr != nil {
				@components.CallToAction()
				<div class="page-content-container">
					<p class="root-page-error">{ rootEventsLoadErrorMessage }</p>
				</div>
			} else if len(programDays) > 0 {
				{{ selectedDayIndex := program.SelectDayIndex(programDays, requestedDate, time.Now()) }}
				@programDaySelector(programDays, selectedDayIndex)
				@components.CallToAction()
				{{ selectedDay := programDays[selectedDayIndex] }}
				@eventList(db, eventImageDir, true, &selectedDay, interests)
			} else {
				@components.CallToAction()
				@eventList(db, eventImageDir, true, nil, interests)
			}
		} else {
			@components.CallToAction()
			@eventList(db, eventImageDir, false, nil, interests)
		}
	}
}
