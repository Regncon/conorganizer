# The Caddyfile is an easy way to configure your Caddy web server.
#
# Unless the file starts with a global options block, the first
# uncommented line is always the address of your site.
#
# To use your own domain name (with automatic HTTPS), first make
# sure your domain's A/AAAA DNS records are properly pointed to
# this machine's public IP, then replace ":80" below with your
# domain name.
#
# Global options (optional; keep simple for now)
{
	# email you want Let's Encrypt notices sent to:
	email cinmay05@gmail.com
}
meetup-january-2026.lekeplassen.regncon.no {
    reverse_proxy 127.0.0.1:8080
}

grafana.regncon.no {
	reverse_proxy 127.0.0.1:3400
}

program.regncon.no {
    # Maintenance mode:
    # 1. Comment out reverse_proxy below.
    # 2. Uncomment the header/respond lines below.
    #
    # header Content-Type "text/html; charset=utf-8"
    # header Cache-Control "no-store"
    # respond <<HTML
    # <!doctype html>
    # <html lang="en">
    # <head>
    #   <meta charset="utf-8">
    #   <meta name="viewport" content="width=device-width, initial-scale=1">
    #   <title>Down for maintenance</title>
    #   <style>
    #     body {
    #       margin: 0;
    #       min-height: 100vh;
    #       display: grid;
    #       place-items: center;
    #       font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    #       line-height: 1.5;
    #       background: #f7f3ed;
    #       color: #25211c;
    #     }
    #     main {
    #       width: min(90vw, 42rem);
    #       padding: 3rem 1.5rem;
    #       text-align: center;
    #     }
    #     h1 {
    #       margin: 0 0 1rem;
    #       font-size: clamp(2rem, 6vw, 3.5rem);
    #       line-height: 1.1;
    #     }
    #     p {
    #       margin: 0 auto 1.5rem;
    #       max-width: 34rem;
    #       font-size: 1.125rem;
    #     }
    #     hr {
    #       width: 5rem;
    #       margin: 2.5rem auto;
    #       border: 0;
    #       border-top: 1px solid #d8cfc1;
    #     }
    #   </style>
    # </head>
    # <body>
    #   <main>
    #     <section>
    #       <h1>The site is down for maintenance</h1>
    #       <p>We're doing a little work behind the scenes and will be back as soon as we can. Thanks for your patience.</p>
    #     </section>
    #     <hr>
    #     <section lang="nb">
    #       <h1>Siden er nede for vedlikehold</h1>
    #       <p>Vi gjør litt arbeid i kulissene og er snart tilbake. Takk for tålmodigheten.</p>
    #     </section>
    #   </main>
    # </body>
    # </html>
    # HTML 503

    reverse_proxy 127.0.0.1:18856
}

# Load all per-site configs from this directory:
import /etc/caddy/sites-enabled/*.caddy

#:80 {
# Set this path to your site's directory.
#	root * /usr/share/caddy

# Enable the static file server.
#	file_server

# Another common task is to set up a reverse proxy:
# reverse_proxy localhost:8080

# Or serve a PHP site through php-fpm:
# php_fastcgi localhost:9000
#}

# Refer to the Caddy docs for more information:
# https://caddyserver.com/docs/caddyfile
