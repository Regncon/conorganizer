package ticketholder

import "fmt"

type selected string

const (
	TicketHolderSelected   selected = "selected"
	TicketHolderUnselected selected = "notSelected"
)

// Takes a `selected` parameter to highlight the selected ticket holder
templ TicketHolderButton(currentBillettHolder BillettHolder, eventId string) {
	{{ initials := GetInitials(currentBillettHolder.Name) }}
	{{ shortName := GetFirstNameAndLastInitial(currentBillettHolder.Name) }}
	{{ currentBillettHolderJSON, _ := templ.JSONString(currentBillettHolder) }}
	<style>
		.ticket-holder-picker {
            padding-inline: var(--border-outline-width);
			.card-clickable {
                place-content: start;
                height: 3.125rem;
                width: 9.1rem;
            }
		}
	</style>
	<div class="ticket-holder-picker">
		<div
			role="button"
			data-attr:tabindex={ fmt.Sprintf(`$billettHolderId == %d ? '-1' : '0'`, currentBillettHolder.Id) }
			class="card-clickable card-clickable-item"
			data-current-billett-holder={ currentBillettHolderJSON }
			data-class:selected={ fmt.Sprintf("$billettHolderId == %d", currentBillettHolder.Id) }
			data-on:click={ fmt.Sprintf("$billettHolderId = %d; window.conorganizer.billettholderSelection.set(JSON.parse(evt.currentTarget.dataset.currentBillettHolder)); @put('/event/api/%s/interest/selected-interest')", currentBillettHolder.Id, eventId) }
		>
			@NameInitials(shortName, initials, currentBillettHolder.Color)
		</div>
	</div>
}
